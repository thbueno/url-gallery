node_modules/
.plasmo/
build/
*.crx
*.pem

# Generated by scripts/generate-popup-html.mjs — derived from src/style.css,
# regenerated on every build/dev run (see package.json). Not a second source
# of truth (ADR 0007), so it's not checked in.
src/popup.html
